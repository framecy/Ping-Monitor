# 🚀 PingMonitor v2.1.0 大版本更新说明

PingMonitor v2.1.0 是一个专注于**网络可视化体验**和**底层连接组件重构**的重大里程碑版本。在这个版本中，我们重新设计了状态栏的展示形态，大幅度扩充了网速监控功能，彻底移除了烦人的系统权限弹窗，并对全应用的数据图表进行了质感上的飞跃升级。

---

## ✨ 核心亮点 (Key Features)

### 📈 1. 史诗级增强的状态栏网速监控
状态栏是 PingMonitor 的灵魂所在。在这个版本中，我们将状态栏从单纯的“延迟展示”升级为了强大的**复合网络仪表盘**：
- **精准的上下行网速呈现**：引入了极具苹果原生风格的堆叠网速显示（↑绿色上行 / ↓蓝色下行），让你时刻掌握后台网络吞吐。
- **高阶定制自由度**：由于多面板共存容易导致状态栏空间拥挤或变形，我们开放了“魔改”级别的自定义：
  - **总宽度调节**（80pt - 250pt）
  - **组件限制约束**：内置了最小展示1个、最大展示3个元素的视觉保护逻辑。
  - **字体与对齐微调**：支持 6-18pt 字号、三档字体粗细调节；重写了渲染核心引擎(`.kern` 字距与 `.byClipping` 截断排版)，**彻底告别 macOS 原生组件导致的图文错位问题**，支持是否显示“地球”图标。

### 📊 2. 全新「网速监控」专属页面
你不只可以在状态栏撇一眼网速，更可以在主面板中深入分析流量走向：
- **实时流量统计面板**：提供“今日总下载”、“今日总上传”、“总流量”统计概览。
- **历史流量趋势图**：引入了类似股票软件的趋势折线分析图表，支持通过 **30分钟 / 1小时 / 24小时 / 7天** 四挡时间切片回顾你的长期网络活动。
- **自定义数据刷新频率**：提供 `1s / 2s / 3s / 5s / 10s` 的多样化采集频次选择，兼顾高频监控需求与低功耗需求。
- **智能网卡优先级**：不再是一长串枯燥的网卡名！现在应用会**自动置顶那些既激活且有流量跑动的网卡**。

### 🔐 3. SSH 安全连接架构重构
修复了之前版本通过 `AppleScript` 唤起 Terminal 所引发的恶性循环权限弹窗 (`-1743` 错误)：
- 抛弃 `AppleScript` 注入，更换为更安全的 `.command` 脚本引导执行模式。
- **自动化密码登录终结**：首次内置了 `expect` 脚本自动交互框架，用户只需在配置页填入服务器密码，PingMonitor 即可为你做到**真正的跨端“一键直连”自动化登录**。

### 🍩 4. 数据图表的美学进化
作为一款颜值向的监控应用，2.1.0 淘汰了由于透视问题经常被遮挡内容的旧版 3D 饼图：
- 替换为苹果原生 `SwiftUI Charts (SectorMark)` 构建的**现代环形状态统计图**。
- 将原本左右挤压的图例交互，重构为“上侧大图+底部分切面细目”的悬浮网格布局，信息呈现更加饱满清晰。

---

## 🛠 其他修复与改进

### 修复 (Fixes)
- **网格 UI 强迫症福音**：修复了在“监控看板”与“主机管理”列表中，如果卡片内的统计数据字数（例如：微秒级的小数点）产生断行，会导致同一行的多个卡片高度“参差不齐”的问题，现在的卡片**严格遵守底部对齐准则**。
- **小组件（Widget）消失之谜**：修复了通过二进制 `.dmg` 分发途径安装时，由于沙盒扩展组的签名授权冲突，导致应用无法在“通知中心小组件库”中被 macOS 检索发现的问题。
- **空状态占位符**：修复了部分页面没有添加监控节点时，系统占位的 `ContentUnavailableView` 偏移不居中的视觉 Bug。

---

### 👉 升级指南
所有在 v2.0+ 系列的用户将自动兼容原有保存的主机/规则配置文件。建议前往**“设置 -> 状态栏”**，重新定义专属于你的网速监控皮肤。  
*下载最新的 `.dmg` 并拖入 `Applications` (应用程序) 即可覆盖升级。*
